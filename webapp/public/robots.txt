User-agent: *
Allow: /
Disallow: /api/
Disallow: /admin/

# The catalog is public and read-only. Please fetch the data from /data/*.json
# (or the GitHub repo) instead of crawling every page.

Sitemap: https://freeitchgames.win/sitemap.xml
